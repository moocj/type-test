# type-test

a cute, minimal typing test — monkeytype-inspired, pastel, no framework, no build step.

**stack:** fastapi + sqlmodel + sqlite · vanilla js (es modules) · chart.js · pytest

## status

| milestone | tickets | version |
|---|---|---|
| m1 backend core | t0-t4 | — |
| m2 test loop | t5-t8 | v0.1, v0.2 |
| m3 look & feel | t9-t11 | v0.3 |
| m4 modes & progress | t12-t13 | — |
| m5 ship | t14-t15 | v1.0 |

## run it

```bash
python -m venv .venv
source .venv/Scripts/activate        # windows git bash (powershell: .venv\Scripts\Activate.ps1)
pip install -r requirements.txt
python -m uvicorn app.main:app --reload   # run from this folder